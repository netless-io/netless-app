{
  "name": "netless-app-monorepo",
  "private": true,
  "devDependencies": {
    "@netless/window-manager": "^0.1.28",
    "@types/node": "^16.7.1",
    "cross-env": "^7.0.2",
    "esbuild": "^0.12.22",
    "rimraf": "^3.0.0",
    "sass": "^1.38.1",
    "svelte": "^3.42.2",
    "typescript": "^4.3.5",
    "vite": "^2.5.0",
    "white-web-sdk": "^2.13.18"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm build -r",
    "dev": "pnpm dev --filter playground"
  }
}
