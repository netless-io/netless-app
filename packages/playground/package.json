{
  "name": "playground",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --open --host",
    "build:ci": "NODE_OPTIONS='--max-old-space-size=6144' vite build",
    "cleanup": "rimraf dist"
  },
  "devDependencies": {
    "@faker-js/faker": "^6.1.1",
    "@netless/app-embedded-page-sdk": "workspace:*",
    "@netless/app-shared": "workspace:*"
  }
}
