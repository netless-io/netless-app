{
  "name": "playground",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --open --host",
    "build:ci": "vite build",
    "cleanup": "rimraf dist"
  },
  "devDependencies": {
    "@netless/app-embedded-page-sdk": "workspace:*",
    "@netless/app-shared": "workspace:*",
    "@types/faker": "^5.5.9",
    "faker": "^5.5.3"
  }
}
