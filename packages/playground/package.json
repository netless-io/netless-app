{
  "name": "playground",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --open --host",
    "build": "vite build",
    "build:dev": "vite build --mode development"
  },
  "devDependencies": {
    "@netless/app-shared": "workspace:*",
    "@types/faker": "^5.5.8",
    "faker": "^5.5.3"
  }
}
