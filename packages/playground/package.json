{
  "name": "playground",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --open --host",
    "build:ci": "vite build",
    "cleanup": "rimraf dist"
  },
  "devDependencies": {
    "@faker-js/faker": "^6.0.0-alpha.6",
    "@netless/app-embedded-page-sdk": "workspace:*",
    "@netless/app-shared": "workspace:*"
  }
}
